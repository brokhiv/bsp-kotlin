import org.junit.jupiter.api.Test

class DFTTest {
    // Will always succeed if it runs, make sure to check the result manually
    @Test
    fun `Create DFT from RE, check manually`() {
        println(constructDFT(InitialFollowTest.mre))
    }
}